-- LazyVim entrypoint (launched via `vim` / `lvim`, i.e. NVIM_APPNAME=lvim).
-- Plain `nvim` uses the default config dir and stays vanilla.
-- Edit files under lua/config/ and lua/plugins/ to customise.
require("config.lazy")
